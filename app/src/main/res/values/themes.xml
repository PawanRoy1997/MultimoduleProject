<resources>
    <!-- Base application theme. -->
    <style name="Theme.MultimoduleProject" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_normal</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/primary_light</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary_normal</item>
        <item name="colorSecondaryVariant">@color/secondary_dark</item>
        <item name="colorOnSecondary">@color/secondary_light</item>
        <item name="android:textSize">30sp</item>
        <!-- Status bar color. -->

        <!-- Customize your theme here. -->
        <item name="android:textViewStyle">@style/MediumTextView</item>
        <item name="android:buttonStyle">@style/ButtonStyle</item>
        <item name="civ_border_color">@color/primary_dark</item>
    </style>
</resources>